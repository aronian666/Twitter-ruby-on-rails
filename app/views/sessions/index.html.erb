<div class="login">
  <div class="left">
    <div class="">
      <div class="left-item">
        <i class='icon search'></i>
        Sigue lo que te interesa.
      </div>
      <div class="left-item">
        <i class="users icon"></i>
        Entérate de lo que está hablando la gente.
      </div>
      <div class="left-item">
        <i class='icon comment outline'></i>
        Únete a la conversación
      </div>
    </div>
  </div>
  <div class="login-form">
    <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@user.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @user.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
    <%= form_for(:session, url: login_path) do |f| %>
      <%= f.email_field :email, class: 'login-input', placeholder: 'Correo electrónico' %>
      <%= f.password_field :password, class: 'login-input', placeholder: 'Contraseña' %>
      <%= f.submit 'Iniciar sesión', class: 'login-button' %>
    <% end %>
    <div class="login-details">
      <div class="">

      </div>
      <h1>Descubre lo que está pasando en el mundo en este momento</h1>
      <div class="">
        <h4>Únete hoy a Algo</h4>
        <%=link_to 'Registrarse' ,'/signup', class: 'login-button registrarse' %>
      </div>
    </div>
  </div>
</div>
